class Signal {
    constructor(value) {
        this.value = String(value);  // Convertir en chaîne de caractères
    }

    setValue(value) {
        this.value = String(value);  // Convertir en chaîne de caractères
    }
}

export default Signal;
